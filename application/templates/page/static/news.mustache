<h3>ERMAGERD NERWS</h3>
<p>Events site beta testing.  There are a couple of known bugs, but go on and report anything you find.</p>

<p>Changelog 2012 Nov 12<p>
<ul>
	<li>Fixed a bug that was intermittently preventing character creation / editing</li>
	<li>Added filters to prevent extraneous spaces on some fields that prevented form validation.</li>
	<li>Changed server environment var to testing, which disabled the debugger toolbar (RIP).</li>
	<li>Numerous tiny CSS tweaks</li>
	<li>Fixed a bug which prevented enrollment numbers from displaying properly in the event list</li>
</ul>

<p>Known bugs</p>
<ul>
	<li>There is a bug that causes the wrong event action (enroll / withdraw) from showing (but only sometimes).  Still tracking this one down.
	<li>The enrollment comment icon gets shifted verticall sometimes.  Need to rewrite the css for that</li>
</ul>

<p>Planned features</p>
<ul>
	<li>Calendar view option for events</li>
	<li>Guild tag support</li>
	<li>Guild invite request</li>
</ul>